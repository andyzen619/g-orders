/* eslint-disable react/prop-types */
/* eslint-disable react/display-name */
import React from 'react';

import NewOrderNavbarView from './NewOrderNavbarView';

export default ({states}) => {
  return <NewOrderNavbarView states = {states}/>;
};
