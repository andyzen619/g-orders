import React from 'react';

const NewOrderButton = () => {
  return (
    <div
      // eslint-disable-next-line max-len
      className='flex justify-center bg-gray-600 m-4 p-2 rounded text-white font-extrabold text-'>
      New Order
    </div>
  );
};

export default NewOrderButton;
